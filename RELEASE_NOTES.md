# Lawyer Assistance 1.2.1 发布说明

发布日期：2026-09-12。本版本修复 1.2.0 审查及独立复测发现的问题，保留 WebUI、本地服务、MCP 和加密工作区架构。

[下载 v1.2.1](https://github.com/shilittle/Lawyer-Assistance/releases/tag/v1.2.1)：Windows x86_64 完整便携包、源码包、SHA-256 校验文件和资源清单。资产文件名附构建日期与源码提交，具体身份以随包清单为准。

## 主要变化

- 文书编辑、保存和导出绑定固定文书及版本；隔离迟到响应，保存期间继续输入或切换文书不会改变保存、导出的目标。
- 案件事实、生成要求及正文草稿加密保存，支持浏览器重启恢复；双窗口冲突保留独立候选，刷新后可以找回并处理。
- 统一任务轮询、取消及会话失效处理。材料清单由服务器维护版本，移除材料后取消相关任务，并停止沿用依赖这些材料的历史。
- Web、AI、MCP 共用查询语义：法律名与条号精确定位，支持全部关键词、任一关键词和完整短语；统一日期及版本范围，修正索引交集、分页与排序。
- 模型能力区分未知、支持和不支持；容量独立配置，未核实容量使用 16k 输入、4k 输出预算。材料可选择页或段落范围，预检、准备计划和实际模型消息绑定所选范围。
- PDF 在受限子进程中处理，文本页本机提取，扫描及混合页按需 OCR；补齐任务准入、取消、资源限制及安全诊断记录。
- 引用核验绑定法规来源、正文和文书版本；编辑后旧检查失效。待复核文书允许导出，但保留真实核验状态，法律适用仍需人工判断。
- 修复预览 HTML 递归净化和公开错误提示，补齐 zune 依赖许可证正文。

## 升级与校验

停止旧服务，完整解压下载的 Windows 便携 ZIP，双击 `Lawyer-Assistance.vbs`。默认继续使用现行 Web 工作区；迁移前创建并验证备份，旧历史目录保留。浏览器关闭不会取消后台已接受的任务。

便携包包含两个程序、法规库、案例库、检索索引、Pdfium、Typst、中文字体、文档、来源清单和许可证。源码包包含相对 v1.2.0 的补丁。核对相邻 `.zip.sha256`、包内 `MANIFEST.sha256` 与 `portable.manifest.json`。程序未签名，不提供安装器或自动更新器。

## 验证范围

前序修复提交 `f60aad6` 的完整验收 43/43 通过，包括 Rust 996 项和 Python 147 项回归，以及前端、Clippy、格式、原生资源与本地服务专项。发布构建的身份及便携包复核结果见 Release 附件 `release-validation.json`。

验证使用隔离工作区、合成材料、公开法规库副本和本地模拟模型，未调用真实付费模型。测试结果不代表真实案件准确率。原始闪退根因未确认；本版本不据故障注入结果宣称已定因。

独立复测的实现与边界见[续修记录](docs/web/retest-1.2.1.md)；[上一轮报告](docs/web/audit-1.2.1.md)保留为历史记录；接口兼容约定见[接口变更](docs/web/audit-1.2.1-contracts.md)。
