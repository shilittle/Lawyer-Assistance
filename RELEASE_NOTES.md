# Lawyer Assistance 1.2.1 本地修复候选版

本次以 `cb085ba` 为基线，修复 1.2.0 审查报告 LA-01～LA-15，并补齐 zune 许可证正文。保留现有 WebUI、本地服务和加密存储架构。便携候选版在本地交付，尚未作为 GitHub Release 发布。

## 使用上的变化

- 文书编辑、保存和导出绑定固定任务版本；正文或日期改变后，旧引用检查立即失效。待复核文书可以导出，相关性与法律适用仍须人工判断。
- 案件事实、生成要求及未提交正文自动保存到加密工作区，刷新或重启浏览器后恢复；成功生成不会删除草稿。
- 普通检索默认全部关键词，支持任一关键词和完整短语。法律名加条号执行精确定位；无日期默认当前有效版本，历史范围必须明确选择。
- 会话只发送服务器确认的材料清单。移除材料会取消仍在使用它的任务，并停止在后续请求中沿用依赖该材料的历史。
- 任务和检索超过容量时明确提示，网络异常采用有限重试。关闭浏览器不取消后台已接受任务。
- PDF 逐页处理：文本页本机提取，扫描或混合页按需 OCR。原生文档处理在受限子进程中运行，子进程失败不能拖垮主服务。
- 模型输入、输出及工具请求有预算。未配置模型容量时采用保守的 16k 输入和 4k 输出预算，界面显示未核实；长材料的采用范围和省略原因可以查看。

## 升级与交付

停止旧服务，完整解压 `Lawyer-Assistance_1.2.1_windows-x86_64-portable.zip`，双击 `Lawyer-Assistance.vbs`。默认继续使用现行 Web 工作区。升级只处理实际打开的工作区，迁移前创建并验证可恢复备份；旧历史目录保留。

便携包包含两个程序、法规库、案例库、派生检索索引、Pdfium、Typst、中文字体、来源清单和许可证。核对相邻 `.zip.sha256`、包内 `MANIFEST.sha256` 与 `portable.manifest.json`。包内不包含用户工作区、凭据或模型原始消息。

验证使用隔离工作区、合成材料、本地模拟模型及只读完整法规库。各项通过情况、保留的失败证据和限制以[逐项修复与测试报告](docs/web/audit-1.2.1.md)为准；接口兼容约定见[接口变更](docs/web/audit-1.2.1-contracts.md)。原始 Windows 闪退根因尚未确认，注入文档子进程故障的恢复验证不能替代原始崩溃证据。
