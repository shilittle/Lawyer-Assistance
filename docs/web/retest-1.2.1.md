# 1.2.1 独立复测后续修复记录

本轮以 `df1a000ad9418c6c716cf5478b86524e5a3b5d6c` 为基线，按独立复测工作单继续修补。当前为实施记录，尚未完成最终验收。上一轮报告、源码包和原始日志保留；其中“已验证”的范围不能覆盖本次重新打开的缺陷。

## 证据来源与边界

用户提供的独立复测报告、下一步工作单及源码摘录按原始字节保存并计算 SHA-256。报告引用的原始证据 ZIP、JSON 和脚本未取得；本轮根据报告重建失败断言，先验证基线失败，再使用相同断言验证候选。重建结果不标为原始独立复测结果。

开发及验收只使用隔离合成工作区、合成材料、公开法规库副本和 localhost mock。当前未调用真实付费模型、迁移用户现行工作区、发布 GitHub Release或启用崩溃转储。

## 当前基线

- 已保存基线源码 Git 归档、两个历史 release 程序，以及锁文件、公开数据库、Pdfium、Typst 和字体哈希。
- 前端原有 29 项测试通过，不能覆盖本次发现的乱序响应缺陷。
- 本机完整 Python 83 项最初为 81 通过、2 失败：集成校验器陈旧 5/8 工具假设，以及来源清单工作树 CRLF 与绑定的 LF 字节不一致。
- 来源清单已按 Git 提交的 LF 字节恢复；确认只有换行变化，未更改原始字节哈希算法或分发证据值。
- 重建能力/错误目录/净化契约测试 16 项均在基线失败，结果保留于本轮证据目录。
- 重建编辑器回归的 8 项缺陷在工作树和基线 Git 原始字节检出中均失败；原始脚本、JSON 和截图已单独保留。新增冲突恢复、双窗口及导出检查另列为修后门禁，不计入修前通过数。
- 两项真实 Rust 检索测试在基线失败：硬编码法名加分改变排序，以及两个各有 901 个候选的词在交集仅 1 条时仍回退。旧二进制的原生文书测试也确认缺少稳定 `document_id`。

## 分批门禁记录

阶段 0 已通过：原有 13 个 Python 模块 83/83，通过新增验收器的 8 项单元测试；环境、资源与字节预检 12/12。刻意移除 Node 的环境得到 `blocked`，其依赖检查标记 `not_run`，没有伪装成完成。新验收器会动态纳入新增 Python 模块；完整验收不能只引用本节的预检结果。

阶段 1 已通过对应门禁：工作区服务 123 项 Rust 测试（79 项单元、30 项 AI 工作流、14 项工作流）和相关 Clippy 通过；前端 31 项单元测试、20 项确定性 Chromium 场景及原有 UI 回归通过。最终嵌入程序重新验证了浏览器进程重启后的加密草稿恢复，以及后台已保存 A、界面切到 C 时仍导出 A 的固定版本并保留 C 输入。原生草稿接口覆盖稳定文书标识、继续任务、CAS 候选、分页及 TXT/PDF/DOCX 正文。

独立代码复核还发现并修复了冲突动作迟到回写、导出选项跨文书、保存期间日期变化和第 21 个候选不可恢复等边界。最后一个未缓存旧版本恢复断言在 iteration8 失败，iteration13 的相同逻辑文书校验通过；失败报告均保留。阶段 1 的结果不能替代后续范围选择、原生故障及全工作区最终验收。

阶段 2 已通过对应门禁：工作区服务 137 项 Rust 测试、相关 Clippy 与格式检查通过；前端 33 项单元、20 项编辑器回归、6 项范围控件回归和 14 项能力/错误目录契约通过。完整 Python 已纳入 14 个模块，93/93 通过。真实 daemon 验证范围 13/13、能力配置 6/6、引用与上下文 13/13；新选页 Pdfium 回归也使用实际 DLL 通过。

本批复测中修正了选中页数与 PDF 总页数误比导致的渲染失败，并从截图发现完成后仍显示旧预检的问题。界面失败断言为 12 通过、1 失败，修后相同原生流程 13/13；完成态显示后台实际采用的段落。Provider 额外补齐配置/能力/默认选择的事务保存、凭据快照读取和不可验证补偿后的持久阻断；容量单独更新、重启和重新配置都有回归。相关失败日志均保留，不能用早期通过结果覆盖这些后续发现。

完整公开库基线使用独立字节副本。在本次环境下，原版 100 次分页请求的首次查询键 p50/p95 为 1895/3750 ms，热页为 2.4/141.7 ms，新页为 1815/1990 ms，峰值 RSS 54,702,080 bytes。该测量不清空操作系统缓存，不称为冷盘测试；候选对比尚未完成。

## 项目状态

| 项目 | 问题 | 当前状态 |
| --- | --- | --- |
| R01 / LA-01 | 迟到保存错绑其他文书 | 已修复；确定性浏览器与原生导出通过 |
| R02 / LA-02 | 历史点击覆盖输入与详情乱序 | 已修复；乱序、输入代数及浏览器重启恢复通过 |
| R03 / LA-03 | 运行中会话失效未清理 | 已修复；会话 epoch、读取中止与重新登录回归通过 |
| R04 / LA-02 | 草稿 CAS 冲突无法恢复 | 已修复；双窗口、再次冲突、分页、刷新与旧版本绑定通过 |
| R05 / LA-04 | 民法典硬编码排序加分残留 | 重新打开，待修复与原生检索验收 |
| R06 / LA-07 | 跨词 ALL 联合约束前过早回退 | 重新打开，待修复与分页性能测量 |
| R07 / LA-14 | 未知能力错误写为 false | 已修复；三态、容量独立更新、旧值保留与原生重启通过 |
| R08 / LA-10 | 十二个公开错误缺少对应提示 | 已修复；公开错误目录契约通过 |
| R09 / LA-13 | 未保存正文的导出目标不清晰 | 已修复；保存快照与已保存版本导出通过 |
| R10 | 未知 HTML 容器展开后漏检子节点 | 修前契约失败已记录，生产 CSP 验收待补 |
| R11 | 预检、完整 Python 与 CI 覆盖缺口 | 实施中 |
| LA-14 范围选择 | 页/段落选择界面及准备绑定缺失 | 已修复；真实 PDF、文本选择、预算、准备失效和实际外发范围通过 |
| 原生稳定性 | 真实 Job 越限、恢复及迁移门禁 | 待执行，配置读回不视为越限通过 |
| 原始闪退 | 用户原始崩溃根因 | 未确认；不与故障注入结果混同 |

本轮最终状态将逐项绑定源码、程序与资源哈希及测试日志；未执行项不会沿用上一轮通过数。
