# OpenCode MCP examples

`opencode.local.json` and `opencode.remote.json` expose only public legal research. `opencode.privacy-workspace.local.json` is disabled by default and requires a local backend plus a token file.
